machine:
  java:
    version: oraclejdk8
dependencies:
 override: 
  - mvn install -DskipTests    
test:
  override:
    - mvn integration-test -Pit
  post:
    - mvn clean install -Pit cobertura:cobertura org.eluder.coveralls:coveralls-maven-plugin:report -X -DrepoToken=wex90LJ7NyAHoySxGTl33uhFVdyGk7EEg

